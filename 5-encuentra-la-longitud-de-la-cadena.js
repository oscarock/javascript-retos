// es crear una función longitud que encuentre la longitud de una cadena (string).

// Ejemplos:

// longitud("") //=> 0
// longitud("Hola") //=> 4
// longitud("You Rock") //=> 8 (contando el espacio)

// mi solucion
function longitud(string){
	return string.length
}